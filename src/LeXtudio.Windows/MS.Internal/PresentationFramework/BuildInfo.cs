namespace MS.Internal.PresentationFramework;

// Constants used by upstream TypeConverter attribute strings. Mirrors the
// values in WPF's Shared/RefAssemblyAttrs.cs (not linked because that file
// also carries assembly-level attributes).
internal static class BuildInfo
{
    internal const string WCP_VERSION = "4.0.0.0";
    internal const string WCP_PUBLIC_KEY_TOKEN = "31bf3856ad364e35";
}
